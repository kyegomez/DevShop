# Core dependencies for the Concurrent Multi-App Generator
pandas>=2.0.0
numpy>=1.24.0

# For async operations
asyncio-throttle>=1.0.2

# For web scraping and API calls (if needed by generated apps)
requests>=2.31.0
aiohttp>=3.8.0

# For GitHub and Vercel API integration
PyGithub>=1.59.0
httpx>=0.24.0

# For working with different file formats
openpyxl>=3.1.0  # Excel support
PyYAML>=6.0      # YAML support

# For enhanced logging and progress tracking
loguru>=0.7.0
tqdm>=4.65.0
rich>=13.0.0

# Claude Code SDK (when available)
# claude-code-sdk  # Uncomment when installing from PyPI

# Development and testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Optional: For enhanced CSV processing
chardet>=5.2.0  # Character encoding detection

swarms
